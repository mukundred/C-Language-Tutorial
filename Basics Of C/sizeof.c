#include<stdio.h>
int main()
{
    // This is used to find the size of a DATA TYPE !!
    /*
    sizeof() is an operator
    */
printf("%zu\n", sizeof(int));
printf("%zu\n", sizeof(char));
printf("%zu\n", sizeof(double));
printf("%zu\n", sizeof(float));
return 0;
}